import React from 'react';
import Banner from '../Banner/Banner';
import Serve from '../Serve/Serve';

const Home = () => {
    return (
        <div>
            <Banner /> 
            <Serve />
        </div>
    );
};

export default Home;